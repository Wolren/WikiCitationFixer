{
  "name": "wikifix-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run build && npm test",
    "ci": "npm run build && npm test"
  },
  "devDependencies": {
    "adm-zip": "^0.5.17",
    "archiver": "^8.0.0",
    "esbuild": "^0.25.0",
    "jsdom": "^29.1.1",
    "jszip": "^3.10.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "isbn3": "^2.0.8"
  }
}
